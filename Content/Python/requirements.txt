# VibeUE Python MCP Server Dependencies
# Install with: pip install -r requirements.txt

# Core MCP dependencies
# Note: fastmcp 2.x has breaking changes with type annotations
# If using from __future__ import annotations, ensure compatibility
mcp>=1.4.1,<2.0.0
fastmcp>=2.0.0,<3.0.0

# Web server dependencies
uvicorn
fastapi

# Data validation and HTTP
pydantic>=2.6.1
requests

# Optional: SVG to PNG conversion support
# Required for manage_asset svg_to_png action
# Install with: pip install cairosvg pillow numpy
# Or with uv: uv pip install cairosvg pillow numpy
# cairosvg>=2.8.0
# pillow>=11.0.0
# numpy>=1.26.0

# Optional development dependencies
# Uncomment if you plan to develop or extend VibeUE
# pytest>=7.0.0
# black>=22.0.0
# mypy>=1.0.0